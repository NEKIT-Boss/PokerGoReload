<Page
    x:Class="PokerGo.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PokerGo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="using:Prism.Windows.Mvvm"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Text="{x:Bind ViewModel.SuccessCounter, Mode=OneWay}"/>
        <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Text="{x:Bind ViewModel.FailCounter, Mode=OneWay}" 
                   Foreground="Red"
                   Margin="0 20 0 0"/>
    </StackPanel>
</Page>
